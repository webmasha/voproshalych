# PostgreSQL with pgvector (standard working image)
# Note: For full Graph support with LightRAG, we'd need Apache AGE extension
# For now: Vector storage via pgvector, Graph via NetworkXStorage

FROM pgvector/pgvector:pg16
